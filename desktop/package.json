{
  "name": "parkir-desktop",
  "version": "0.1.0",
  "private": true,
  "main": "dist/main.js",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "npm run build && electron .",
    "build": "tsc && npm run copy-renderer",
    "copy-renderer": "cp src/renderer/index.html dist/renderer/index.html",
    "lint": "echo 'lint not configured yet'",
    "start": "electron ."
  },
  "dependencies": {
    "electron": "^31.0.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "typescript": "^5"
  }
}
